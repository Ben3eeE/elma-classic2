#ifndef BADGE_MENU_H
#define BADGE_MENU_H

// Show the level selector menu. Returns when a level is selected.
// Returns the internal level index (1-55), or 0 to quit.
int badge_level_selector();

#endif
